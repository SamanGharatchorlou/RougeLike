#include "pch.h"
#include "Effect.h"

#include "Objects/Actors/Actor.h"


//bool Effect::hasProperty(const Actor* distributer, const std::string property)
//{
//	if (distributer->hasProperty(property))
//		return true;
//
//	DebugPrint(Warning, "Effect distributer '%s' has no %s property\n", distributer->collider()->name().c_str(), property.c_str());
//	return false;
//}